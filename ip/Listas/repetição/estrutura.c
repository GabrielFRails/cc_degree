#include <stdio.h>
#include <math.h>

/**
2 * Imprime os bits dos n bytes endereçados por end_byte.
3 * @param end_byte endereço do primeiro byte a ser impresso
4 * @param quantidade de bytes a serem impressos
5 */

void print_bytes( const void * end_byte, int n );

int main(){
    unsigned char uc; unsigned short us; unsigned int ui; float f;
    double v;
    scanf("%lf", &v);
    uc = us = ui = f = v;
    

    return 0;
}